This is version 2.8.0 of xercesc
that we copied here to avoid 
occasional problems with the access to 
CVS repositories used by the apache project. 
This snapshot contains just the sources to 
build the library where we stripped the version 
string from the directory name.  This 
snapshot does not contain the documentation, 
tests, examples etc. 
